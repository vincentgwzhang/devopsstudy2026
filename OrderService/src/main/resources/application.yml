server:
  port: 8081

spring:
  main:
    banner-mode: "off"
  application:
    name: OrderService

logging:
  level:
    root: WARN
    com.observe.order: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  metrics:
    tags:
      application: ${spring.application.name}
  otlp:
    metrics:
      export:
        enabled: true
        url: http://localhost:4318/v1/metrics
        step: 5s
  tracing:
    sampling:
      probability: 1.0

services:
  payment:
    url: http://localhost:8082
